<!-- NAVIGATION -->
<nav id="navigation">
    <!-- container -->
    <div class="container">
        <!-- responsive-nav -->
        <div id="responsive-nav">
            <!-- NAV -->
            <ul class="main-nav nav navbar-nav">
                <li class="@(ViewData["ActiveMenu"]?.ToString() == "Home" ? "active" : "")">
                    <a asp-controller="Home" asp-action="Index">Home</a>
                </li>
                <li class="@(ViewData["ActiveMenu"]?.ToString() == "Products" ? "active" : "")">
                    <a asp-controller="Product" asp-action="Index">Smartphones</a>
                </li>
                <li class="@(ViewData["ActiveMenu"]?.ToString() == "Categories" ? "active" : "")">
                    <a asp-controller="Category" asp-action="Index">Categories</a>
                </li>
                <li class="@(ViewData["ActiveMenu"]?.ToString() == "About" ? "active" : "")">
                    <a asp-controller="Home" asp-action="AboutUs">About Us</a>
                </li>
                <li class="@(ViewData["ActiveMenu"]?.ToString() == "Contact" ? "active" : "")">
                    <a asp-controller="Contact" asp-action="Index">Contact</a>
                </li>
            </ul>
            <!-- /NAV -->
        </div>
        <!-- /responsive-nav -->
    </div>
    <!-- /container -->
</nav>
<!-- /NAVIGATION -->